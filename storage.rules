
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // The default rule allows read and write access only to authenticated users.
    // This is a good starting point for most applications.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
