'use client';

import { SoloCampaignStepper } from '@/components/campaign/solo-campaign-stepper';

export default function SoloCampaignPage() {
  // This page will now orchestrate the state and logic for the solo campaign creation.
  return <SoloCampaignStepper />;
}
